import React from "react";
import ProjectCard from "../../home/recent-work/project-card";

const SpecializedCard = ({ project }: { project: number }) => {
  return <ProjectCard project={project} />;
};

export default SpecializedCard;
