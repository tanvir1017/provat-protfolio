import ProjectCard from "../../home/recent-work/project-card";

const SpecializedCardManufacture = ({ project }: { project: number }) => {
  return <ProjectCard project={project} />;
};

export default SpecializedCardManufacture;
